/**\n * Achievement System API Functions\n * Handles all achievement-related API calls\n */\n\nimport { API_BASE_URL } from '../config/api';\n\n// Get all achievements\nexport const getAchievements = async (category = null, difficulty = null, limit = 100, offset = 0) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString(),\n      offset: offset.toString()\n    });\n    \n    if (category) {\n      params.append('category', category);\n    }\n    \n    if (difficulty) {\n      params.append('difficulty', difficulty);\n    }\n\n    const response = await fetch(`${API_BASE_URL}/api/achievements?${params}`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievements');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievements:', error);\n    throw error;\n  }\n};\n\n// Get achievement by ID\nexport const getAchievementById = async (achievementId) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/achievements/${achievementId}`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement:', error);\n    throw error;\n  }\n};\n\n// Get user achievements\nexport const getUserAchievements = async (userId, token, limit = 50, offset = 0) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString(),\n      offset: offset.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch user achievements');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching user achievements:', error);\n    throw error;\n  }\n};\n\n// Get user achievement progress\nexport const getUserAchievementProgress = async (userId, token, achievementId) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/${achievementId}/progress`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement progress');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement progress:', error);\n    throw error;\n  }\n};\n\n// Update achievement progress\nexport const updateAchievementProgress = async (userId, token, achievementId, progress, increment = false) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/${achievementId}/progress`, {\n      method: 'PUT',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        progress: progress,\n        increment: increment,\n        updated_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to update achievement progress');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error updating achievement progress:', error);\n    throw error;\n  }\n};\n\n// Unlock achievement\nexport const unlockAchievement = async (userId, token, achievementId, progress = null) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/${achievementId}/unlock`, {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        user_id: userId,\n        achievement_id: achievementId,\n        progress: progress,\n        unlocked_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to unlock achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error unlocking achievement:', error);\n    throw error;\n  }\n};\n\n// Claim achievement reward\nexport const claimAchievementReward = async (userId, token, achievementId) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/${achievementId}/claim`, {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        user_id: userId,\n        achievement_id: achievementId,\n        claimed_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to claim achievement reward');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error claiming achievement reward:', error);\n    throw error;\n  }\n};\n\n// Get achievement statistics\nexport const getAchievementStatistics = async (userId, token) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/statistics`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement statistics');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement statistics:', error);\n    throw error;\n  }\n};\n\n// Get achievement leaderboard\nexport const getAchievementLeaderboard = async (category = null, period = 'all', limit = 100) => {\n  try {\n    const params = new URLSearchParams({\n      period: period,\n      limit: limit.toString()\n    });\n    \n    if (category) {\n      params.append('category', category);\n    }\n\n    const response = await fetch(`${API_BASE_URL}/api/achievements/leaderboard?${params}`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement leaderboard');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement leaderboard:', error);\n    throw error;\n  }\n};\n\n// Get achievement history\nexport const getAchievementHistory = async (userId, token, limit = 50, offset = 0) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString(),\n      offset: offset.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/history?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement history');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement history:', error);\n    throw error;\n  }\n};\n\n// Get achievement recommendations\nexport const getAchievementRecommendations = async (userId, token, limit = 10) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/recommendations?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement recommendations');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement recommendations:', error);\n    throw error;\n  }\n};\n\n// Get next achievements\nexport const getNextAchievements = async (userId, token, limit = 5) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/next?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch next achievements');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching next achievements:', error);\n    throw error;\n  }\n};\n\n// Check for new achievements\nexport const checkNewAchievements = async (userId, token) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/check`, {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        checked_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to check for new achievements');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error checking for new achievements:', error);\n    throw error;\n  }\n};\n\n// Get achievement categories\nexport const getAchievementCategories = async () => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/achievements/categories`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement categories');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement categories:', error);\n    throw error;\n  }\n};\n\n// Get achievement difficulties\nexport const getAchievementDifficulties = async () => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/achievements/difficulties`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement difficulties');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement difficulties:', error);\n    throw error;\n  }\n};\n\n// Search achievements\nexport const searchAchievements = async (query, category = null, difficulty = null, limit = 20) => {\n  try {\n    const params = new URLSearchParams({\n      query: query,\n      limit: limit.toString()\n    });\n    \n    if (category) {\n      params.append('category', category);\n    }\n    \n    if (difficulty) {\n      params.append('difficulty', difficulty);\n    }\n\n    const response = await fetch(`${API_BASE_URL}/api/achievements/search?${params}`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to search achievements');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error searching achievements:', error);\n    throw error;\n  }\n};\n\n// Get achievement trends\nexport const getAchievementTrends = async (period = 'week', limit = 10) => {\n  try {\n    const params = new URLSearchParams({\n      period: period,\n      limit: limit.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/achievements/trends?${params}`, {\n      method: 'GET',\n      headers: {\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement trends');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement trends:', error);\n    throw error;\n  }\n};\n\n// Get achievement analytics\nexport const getAchievementAnalytics = async (userId, token, period = 'week') => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/analytics?period=${period}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement analytics');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement analytics:', error);\n    throw error;\n  }\n};\n\n// Share achievement\nexport const shareAchievement = async (userId, token, achievementId, platform = 'general') => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/${achievementId}/share`, {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        platform: platform,\n        shared_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to share achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error sharing achievement:', error);\n    throw error;\n  }\n};\n\n// Get achievement notifications\nexport const getAchievementNotifications = async (userId, token, limit = 20, offset = 0) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString(),\n      offset: offset.toString()\n    });\n\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/notifications?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement notifications');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement notifications:', error);\n    throw error;\n  }\n};\n\n// Mark achievement notification as read\nexport const markAchievementNotificationRead = async (userId, token, notificationId) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/users/${userId}/achievements/notifications/${notificationId}/read`, {\n      method: 'PUT',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        read_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to mark notification as read');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error marking notification as read:', error);\n    throw error;\n  }\n};\n\n// Admin functions\n\n// Get all achievements for admin\nexport const adminGetAchievements = async (userId, token, category = null, difficulty = null, limit = 100, offset = 0) => {\n  try {\n    const params = new URLSearchParams({\n      limit: limit.toString(),\n      offset: offset.toString()\n    });\n    \n    if (category) {\n      params.append('category', category);\n    }\n    \n    if (difficulty) {\n      params.append('difficulty', difficulty);\n    }\n\n    const response = await fetch(`${API_BASE_URL}/api/admin/achievements?${params}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievements for admin');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievements for admin:', error);\n    throw error;\n  }\n};\n\n// Create achievement\nexport const adminCreateAchievement = async (userId, token, achievementData) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/admin/achievements/create`, {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        ...achievementData,\n        created_by: userId,\n        created_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to create achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error creating achievement:', error);\n    throw error;\n  }\n};\n\n// Update achievement\nexport const adminUpdateAchievement = async (userId, token, achievementId, achievementData) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/admin/achievements/${achievementId}`, {\n      method: 'PUT',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n      body: JSON.stringify({\n        ...achievementData,\n        updated_by: userId,\n        updated_at: Date.now()\n      }),\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to update achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error updating achievement:', error);\n    throw error;\n  }\n};\n\n// Delete achievement\nexport const adminDeleteAchievement = async (userId, token, achievementId) => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/admin/achievements/${achievementId}`, {\n      method: 'DELETE',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      const errorData = await response.json();\n      throw new Error(errorData.message || 'Failed to delete achievement');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error deleting achievement:', error);\n    throw error;\n  }\n};\n\n// Get achievement analytics for admin\nexport const adminGetAchievementAnalytics = async (userId, token, period = 'week') => {\n  try {\n    const response = await fetch(`${API_BASE_URL}/api/admin/achievements/analytics?period=${period}`, {\n      method: 'GET',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'x-user-id': userId,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (!response.ok) {\n      throw new Error('Failed to fetch achievement analytics for admin');\n    }\n\n    return await response.json();\n  } catch (error) {\n    console.error('Error fetching achievement analytics for admin:', error);\n    throw error;\n  }\n};\n\nexport default {\n  getAchievements,\n  getAchievementById,\n  getUserAchievements,\n  getUserAchievementProgress,\n  updateAchievementProgress,\n  unlockAchievement,\n  claimAchievementReward,\n  getAchievementStatistics,\n  getAchievementLeaderboard,\n  getAchievementHistory,\n  getAchievementRecommendations,\n  getNextAchievements,\n  checkNewAchievements,\n  getAchievementCategories,\n  getAchievementDifficulties,\n  searchAchievements,\n  getAchievementTrends,\n  getAchievementAnalytics,\n  shareAchievement,\n  getAchievementNotifications,\n  markAchievementNotificationRead,\n  adminGetAchievements,\n  adminCreateAchievement,\n  adminUpdateAchievement,\n  adminDeleteAchievement,\n  adminGetAchievementAnalytics\n};
