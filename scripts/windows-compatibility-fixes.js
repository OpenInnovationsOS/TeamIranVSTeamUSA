#!/usr/bin/env node\n\n// Windows Compatibility Fixes for Production Deployment\n// Resolves Windows-specific issues in production-deploy.js\n\nconst fs = require('fs');\nconst path = require('path');\nconst { execSync } = require('child_process');\nconst os = require('os');\n\n// Windows Compatibility Configuration\nconst WINDOWS_CONFIG = {\n  platform: os.platform(),\n  isWindows: os.platform() === 'win32',\n  commands: {\n    sudo: 'runas',\n    ufw: 'netsh advfirewall',\n    systemctl: 'sc',\n    apt: 'choco',\n    yum: 'choco',\n    curl: 'Invoke-WebRequest',\n    wget: 'Invoke-WebRequest',\n    grep: 'Select-String',\n    ps: 'Get-Process',\n    kill: 'Stop-Process',\n    chmod: 'icacls',\n    chown: 'icacls',\n    tar: 'tar',\n    unzip: 'Expand-Archive'\n  },\n  paths: {\n    config: process.env.APPDATA || 'C:\\\\ProgramData',\n    logs: process.env.LOCALAPPDATA || 'C:\\\\ProgramData\\\\Logs',\n    temp: process.env.TEMP || 'C:\\\\Windows\\\\Temp',\n    programs: 'C:\\\\Program Files',\n    programsx86: 'C:\\\\Program Files (x86)'\n  }\n};\n\n// Windows Compatibility Fixer Class\nclass WindowsCompatibilityFixer {\n  constructor() {\n    this.config = WINDOWS_CONFIG;\n    this.fixes = [];\n    this.startTime = Date.now();\n  }\n\n  // Initialize Windows compatibility fixes\n  async initialize() {\n    console.log('🔧 Initializing Windows Compatibility Fixes...');\n    \n    try {\n      if (!this.config.isWindows) {\n        console.log('✅ Not Windows platform - no fixes needed');\n        return true;\n      }\n\n      await this.detectWindowsVersion();\n      await this.createWindowsScripts();\n      await this.updateProductionDeployScript();\n      await this.createWindowsServiceManager();\n      await this.setupWindowsFirewall();\n      await this.createWindowsDatabaseSetup();\n      await this.testWindowsCompatibility();\n      \n      console.log('✅ Windows Compatibility Fixes Completed Successfully');\n      return true;\n    } catch (error) {\n      console.error('❌ Failed to initialize Windows Compatibility Fixes:', error);\n      return false;\n    }\n  }\n\n  // Detect Windows version\n  async detectWindowsVersion() {\n    console.log('🪟 Detecting Windows version...');\n    \n    try {\n      const version = execSync('ver', { encoding: 'utf8' });\n      console.log(`✅ Windows version detected: ${version.trim()}`);\n      \n      this.config.windowsVersion = version.trim();\n      this.config.isWindows10OrHigher = version.includes('10.') || version.includes('11.');\n      \n      console.log('✅ Windows version detection completed');\n    } catch (error) {\n      console.error('❌ Failed to detect Windows version:', error);\n      this.config.windowsVersion = 'Unknown';\n    }\n  }\n\n  // Create Windows-specific scripts\n  async createWindowsScripts() {\n    console.log('📝 Creating Windows-specific scripts...');\n    \n    // Create Windows deployment script\n    await this.createWindowsDeployScript();\n    \n    // Create Windows service management script\n    await this.createWindowsServiceScript();\n    \n    // Create Windows firewall script\n    await this.createWindowsFirewallScript();\n    \n    // Create Windows database setup script\n    await this.createWindowsDatabaseScript();\n    \n    console.log('✅ Windows-specific scripts created');\n  }\n\n  // Create Windows deployment script\n  async createWindowsDeployScript() {\n    console.log('📝 Creating Windows deployment script...');\n    \n    const windowsDeployScript = `@echo off\nREM Windows Production Deployment Script\nREM Resolves Windows compatibility issues\n\necho 🚀 Starting Windows Production Deployment...\n\nREM Check administrator privileges\nnet session >nul 2>&1\nif %errorLevel% == 0 (\n    echo ✅ Administrator privileges detected\n) else (\n    echo ❌ Administrator privileges required\n    echo Please run as Administrator\n    pause\n    exit /b 1\n)\n\nREM Set environment variables\nset NODE_ENV=production\nset PORT=3001\nset DB_HOST=localhost\nset DB_PORT=5432\nset DB_NAME=teamiranvsteamusa_prod\nset DB_USER=postgres\nset DB_PASSWORD=your_password_here\nset REDIS_HOST=localhost\nset REDIS_PORT=6379\nset REDIS_PASSWORD=your_redis_password_here\n\necho 🔧 Environment variables set\n\nREM Create directories\nif not exist \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\" (\n    mkdir \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\"\n    echo ✅ Created program data directory\n)\n\nif not exist \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\\\\logs\" (\n    mkdir \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\\\\logs\"\n    echo ✅ Created logs directory\n)\n\nif not exist \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\\\\backups\" (\n    mkdir \"C:\\\\ProgramData\\\\TeamIranVSTeamUSA\\\\backups\"\n    echo ✅ Created backups directory\n)\n\necho 📁 Directories created\n\nREM Install dependencies\necho 📦 Installing dependencies...\ncall npm install --production\nif %errorLevel% neq 0 (\n    echo ❌ Failed to install dependencies\n    pause\n    exit /b 1\n)\necho ✅ Dependencies installed\n\nREM Setup database\necho 🗄️ Setting up database...\npsql -h %DB_HOST% -p %DB_PORT% -U %DB_USER% -c \"CREATE DATABASE IF NOT EXISTS %DB_NAME%;\"\nif %errorLevel% neq 0 (\n    echo ❌ Failed to create database\n    pause\n    exit /b 1\n)\n\necho ✅ Database created\n\nREM Run database migrations\necho 🔄 Running database migrations...\ncall npm run migrate:prod\nif %errorLevel% neq 0 (\n    echo ❌ Failed to run migrations\n    pause\n    exit /b 1\n)\n\necho ✅ Database migrations completed\n\nREM Seed database\necho 🌱 Seeding database...\ncall npm run seed:prod\nif %errorLevel% neq 0 (\n    echo ❌ Failed to seed database\n    pause\n    exit /b 1\n)\n\necho ✅ Database seeded\n\nREM Setup Windows Firewall\necho 🛡️ Setting up Windows Firewall...\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTP\" dir=in action=allow protocol=TCP localport=3001\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTPS\" dir=in action=allow protocol=TCP localport=443\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Database\" dir=in action=allow protocol=TCP localport=5432\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Redis\" dir=in action=allow protocol=TCP localport=6379\n\necho ✅ Windows Firewall configured\n\nREM Install Windows Service\necho 🔧 Installing Windows Service...\nsc create \"TeamIranVSTeamUSA\" binPath= \"\\\"%CD%\\\\node.exe\\\" \\\"%CD%\\\\server.js\\\"\"\nsc description \"TeamIranVSTeamUSA\" \"Team Iran vs USA Game Server\"\nsc start \"TeamIranVSTeamUSA\"\nif %errorLevel% neq 0 (\n    echo ❌ Failed to install Windows Service\n    pause\n    exit /b 1\n)\n\necho ✅ Windows Service installed and started\n\nREM Setup monitoring\necho 📊 Setting up monitoring...\ncall npm run monitor:windows\nif %errorLevel% neq 0 (\n    echo ❌ Failed to setup monitoring\n    pause\n    exit /b 1\n)\n\necho ✅ Monitoring setup completed\n\nREM Setup backup\necho 💾 Setting up backup system...\ncall npm run backup:windows\nif %errorLevel% neq 0 (\n    echo ❌ Failed to setup backup\n    pause\n    exit /b 1\n)\n\necho ✅ Backup system setup completed\n\necho 🎉 Windows Production Deployment Completed Successfully!\necho 🌐 Application available at: http://localhost:3001\necho 📊 Monitoring available at: http://localhost:9090\necho 📈 Grafana available at: http://localhost:3001\n\npause\n`;\n\n    const scriptPath = path.join(process.cwd(), 'scripts', 'windows-deploy.bat');\n    fs.writeFileSync(scriptPath, windowsDeployScript);\n    \n    console.log(`✅ Windows deployment script created: ${scriptPath}`);\n  }\n\n  // Create Windows service script\n  async createWindowsServiceScript() {\n    console.log('📝 Creating Windows service script...');\n    \n    const serviceScript = `@echo off\nREM Windows Service Management Script\n\nif \"%1\"==\"install\" (\n    echo 🔧 Installing Windows Service...\n    sc create \"TeamIranVSTeamUSA\" binPath= \"\\\"%CD%\\\\node.exe\\\" \\\"%CD%\\\\server.js\\\"\" start= auto\n    sc description \"TeamIranVSTeamUSA\" \"Team Iran vs USA Game Server\"\n    echo ✅ Service installed\n) else if \"%1\"==\"start\" (\n    echo ▶️ Starting Windows Service...\n    sc start \"TeamIranVSTeamUSA\"\n    echo ✅ Service started\n) else if \"%1\"==\"stop\" (\n    echo ⏹️ Stopping Windows Service...\n    sc stop \"TeamIranVSTeamUSA\"\n    echo ✅ Service stopped\n) else if \"%1\"==\"restart\" (\n    echo 🔄 Restarting Windows Service...\n    sc stop \"TeamIranVSTeamUSA\"\n    timeout /t 5 /nobreak >nul\n    sc start \"TeamIranVSTeamUSA\"\n    echo ✅ Service restarted\n) else if \"%1\"==\"uninstall\" (\n    echo 🗑️ Uninstalling Windows Service...\n    sc stop \"TeamIranVSTeamUSA\"\n    sc delete \"TeamIranVSTeamUSA\"\n    echo ✅ Service uninstalled\n) else (\n    echo Usage: %0% [install^|start^|stop^|restart^|uninstall]\n    echo   install   - Install the Windows service\n    echo   start     - Start the Windows service\n    echo   stop      - Stop the Windows service\n    echo   restart   - Restart the Windows service\n    echo   uninstall - Uninstall the Windows service\n)\n`;\n\n    const scriptPath = path.join(process.cwd(), 'scripts', 'windows-service.bat');\n    fs.writeFileSync(scriptPath, serviceScript);\n    \n    console.log(`✅ Windows service script created: ${scriptPath}`);\n  }\n\n  // Create Windows firewall script\n  async createWindowsFirewallScript() {\n    console.log('📝 Creating Windows firewall script...');\n    \n    const firewallScript = `@echo off\nREM Windows Firewall Configuration Script\n\necho 🛡️ Configuring Windows Firewall...\n\nREM Allow HTTP (port 3001)\nnetsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA HTTP\" >nul 2>&1\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTP\" dir=in action=allow protocol=TCP localport=3001 profile=any\n\necho ✅ HTTP port 3001 allowed\n\nREM Allow HTTPS (port 443)\nnetsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA HTTPS\" >nul 2>&1\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTPS\" dir=in action=allow protocol=TCP localport=443 profile=any\n\necho ✅ HTTPS port 443 allowed\n\nREM Allow Database (port 5432)\nnetsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA Database\" >nul 2>&1\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Database\" dir=in action=allow protocol=TCP localport=5432 profile=any\n\necho ✅ Database port 5432 allowed\n\nREM Allow Redis (port 6379)\nnetsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA Redis\" >nul 2>&1\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Redis\" dir=in action=allow protocol=TCP localport=6379 profile=any\n\necho ✅ Redis port 6379 allowed\n\nREM Allow Monitoring (port 9090)\nnetsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA Monitoring\" >nul 2>&1\nnetsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Monitoring\" dir=in action=allow protocol=TCP localport=9090 profile=any\n\necho ✅ Monitoring port 9090 allowed\n\necho 🎉 Windows Firewall Configuration Completed!\n`;\n\n    const scriptPath = path.join(process.cwd(), 'scripts', 'windows-firewall.bat');\n    fs.writeFileSync(scriptPath, firewallScript);\n    \n    console.log(`✅ Windows firewall script created: ${scriptPath}`);\n  }\n\n  // Create Windows database script\n  async createWindowsDatabaseScript() {\n    console.log('📝 Creating Windows database script...');\n    \n    const databaseScript = `@echo off\nREM Windows Database Setup Script\n\necho 🗄️ Setting up PostgreSQL on Windows...\n\nREM Check if PostgreSQL is installed\npsql --version >nul 2>&1\nif %errorLevel% neq 0 (\n    echo ❌ PostgreSQL not found\n    echo Please install PostgreSQL from https://www.postgresql.org/download/windows/\n    pause\n    exit /b 1\n)\n\necho ✅ PostgreSQL found\n\nREM Create database user\necho 👤 Creating database user...\npsql -U postgres -c \"CREATE USER teamiranvsteamusa WITH PASSWORD 'your_password_here';\"\nif %errorLevel% neq 0 (\n    echo ❌ Failed to create database user\n    pause\n    exit /b 1\n)\n\necho ✅ Database user created\n\nREM Create database\necho 🗄️ Creating database...\npsql -U postgres -c \"CREATE DATABASE teamiranvsteamusa_prod OWNER teamiranvsteamusa;\"\nif %errorLevel% neq 0 (\n    echo ❌ Failed to create database\n    pause\n    exit /b 1\n)\n\necho ✅ Database created\n\nREM Grant privileges\necho 🔐 Granting privileges...\npsql -U postgres -c \"GRANT ALL PRIVILEGES ON DATABASE teamiranvsteamusa_prod TO teamiranvsteamusa;\"\nif %errorLevel% neq 0 (\n    echo ❌ Failed to grant privileges\n    pause\n    exit /b 1\n)\n\necho ✅ Privileges granted\n\nREM Run migrations\necho 🔄 Running database migrations...\nset PGPASSWORD=your_password_here\npsql -h localhost -p 5432 -U teamiranvsteamusa -d teamiranvsteamusa_prod -f migrations/01_init.sql\nif %errorLevel% neq 0 (\n    echo ❌ Failed to run migrations\n    pause\n    exit /b 1\n)\n\necho ✅ Database migrations completed\n\nREM Seed database\necho 🌱 Seeding database...\npsql -h localhost -p 5432 -U teamiranvsteamusa -d teamiranvsteamusa_prod -f seed/seed.sql\nif %errorLevel% neq 0 (\n    echo ❌ Failed to seed database\n    pause\n    exit /b 1\n)\n\necho ✅ Database seeded\n\necho 🎉 Windows Database Setup Completed!\n`;\n\n    const scriptPath = path.join(process.cwd(), 'scripts', 'windows-database.bat');\n    fs.writeFileSync(scriptPath, databaseScript);\n    \n    console.log(`✅ Windows database script created: ${scriptPath}`);\n  }\n\n  // Update production deploy script for Windows compatibility\n  async updateProductionDeployScript() {\n    console.log('📝 Updating production deploy script for Windows compatibility...');\n    \n    const deployScriptPath = path.join(process.cwd(), 'scripts', 'production-deploy.js');\n    \n    if (fs.existsSync(deployScriptPath)) {\n      let deployScript = fs.readFileSync(deployScriptPath, 'utf8');\n      \n      // Replace Linux commands with Windows equivalents\n      deployScript = deployScript.replace(/sudo ufw/g, 'netsh advfirewall');\n      deployScript = deployScript.replace(/sudo systemctl/g, 'sc');\n      deployScript = deployScript.replace(/sudo apt-get/g, 'choco install');\n      deployScript = deployScript.replace(/sudo yum/g, 'choco install');\n      deployScript = deployScript.replace(/curl/g, 'Invoke-WebRequest');\n      deployScript = deployScript.replace(/wget/g, 'Invoke-WebRequest');\n      deployScript = deployScript.replace(/grep/g, 'Select-String');\n      deployScript = deployScript.replace(/ps aux/g, 'Get-Process');\n      deployScript = deployScript.replace(/kill/g, 'Stop-Process');\n      deployScript = deployScript.replace(/chmod/g, 'icacls');\n      deployScript = deployScript.replace(/chown/g, 'icacls');\n      \n      // Add Windows detection at the beginning\n      const windowsDetection = `\n// Windows Compatibility Detection\nconst os = require('os');\nconst isWindows = os.platform() === 'win32';\n\nif (isWindows) {\n  console.log('🪟 Windows detected - using Windows-compatible commands');\n}\n`;\n      \n      deployScript = windowsDetection + deployScript;\n      \n      fs.writeFileSync(deployScriptPath, deployScript);\n      console.log('✅ Production deploy script updated for Windows compatibility');\n    } else {\n      console.log('⚠️ Production deploy script not found');\n    }\n  }\n\n  // Create Windows service manager\n  async createWindowsServiceManager() {\n    console.log('🔧 Creating Windows service manager...');\n    \n    const serviceManager = {\n      name: 'TeamIranVSTeamUSA',\n      displayName: 'Team Iran vs USA Game Server',\n      description: 'Team Iran vs USA Game Server Service',\n      scriptPath: path.join(process.cwd(), 'server.js'),\n      nodePath: process.execPath,\n      workingDirectory: process.cwd(),\n      logPath: path.join(process.cwd(), 'logs', 'service.log'),\n      errorLogPath: path.join(process.cwd(), 'logs', 'service-error.log')\n    };\n    \n    const serviceManagerPath = path.join(process.cwd(), 'scripts', 'windows-service-manager.js');\n    const serviceManagerCode = `\n// Windows Service Manager\nconst { execSync } = require('child_process');\nconst fs = require('fs');\nconst path = require('path');\n\nconst serviceConfig = ${JSON.stringify(serviceManager, null, 2)};\n\nclass WindowsServiceManager {\n  constructor() {\n    this.config = serviceConfig;\n  }\n\n  install() {\n    console.log('🔧 Installing Windows Service...');\n    try {\n      const command = `sc create \"${this.config.name}\" binPath= \"\\\\\"${this.config.nodePath}\\\\\" \\\\\"${this.config.scriptPath}\\\\\"\" start= auto`;\n      execSync(command, { stdio: 'inherit' });\n      \n      const descriptionCommand = `sc description \"${this.config.name}\" \"${this.config.description}\"`;\n      execSync(descriptionCommand, { stdio: 'inherit' });\n      \n      console.log('✅ Windows Service installed successfully');\n    } catch (error) {\n      console.error('❌ Failed to install Windows Service:', error);\n    }\n  }\n\n  start() {\n    console.log('▶️ Starting Windows Service...');\n    try {\n      execSync(`sc start \"${this.config.name}\"`, { stdio: 'inherit' });\n      console.log('✅ Windows Service started successfully');\n    } catch (error) {\n      console.error('❌ Failed to start Windows Service:', error);\n    }\n  }\n\n  stop() {\n    console.log('⏹️ Stopping Windows Service...');\n    try {\n      execSync(`sc stop \"${this.config.name}\"`, { stdio: 'inherit' });\n      console.log('✅ Windows Service stopped successfully');\n    } catch (error) {\n      console.error('❌ Failed to stop Windows Service:', error);\n    }\n  }\n\n  restart() {\n    console.log('🔄 Restarting Windows Service...');\n    this.stop();\n    setTimeout(() => this.start(), 5000);\n  }\n\n  uninstall() {\n    console.log('🗑️ Uninstalling Windows Service...');\n    try {\n      execSync(`sc stop \"${this.config.name}\"`, { stdio: 'inherit' });\n      execSync(`sc delete \"${this.config.name}\"`, { stdio: 'inherit' });\n      console.log('✅ Windows Service uninstalled successfully');\n    } catch (error) {\n      console.error('❌ Failed to uninstall Windows Service:', error);\n    }\n  }\n\n  status() {\n    console.log('📊 Checking Windows Service status...');\n    try {\n      const result = execSync(`sc query \"${this.config.name}\"`, { encoding: 'utf8' });\n      console.log(result);\n    } catch (error) {\n      console.error('❌ Failed to get service status:', error);\n    }\n  }\n}\n\nconst serviceManager = new WindowsServiceManager();\n\nmodule.exports = serviceManager;\n`;\n    \n    fs.writeFileSync(serviceManagerPath, serviceManagerCode);\n    console.log(`✅ Windows service manager created: ${serviceManagerPath}`);\n  }\n\n  // Setup Windows firewall\n  async setupWindowsFirewall() {\n    console.log('🛡️ Setting up Windows firewall...');\n    \n    try {\n      // Allow HTTP port\n      execSync('netsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA HTTP\" >nul 2>&1', { stdio: 'inherit' });\n      execSync('netsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTP\" dir=in action=allow protocol=TCP localport=3001 profile=any', { stdio: 'inherit' });\n      \n      // Allow HTTPS port\n      execSync('netsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA HTTPS\" >nul 2>&1', { stdio: 'inherit' });\n      execSync('netsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA HTTPS\" dir=in action=allow protocol=TCP localport=443 profile=any', { stdio: 'inherit' });\n      \n      // Allow Database port\n      execSync('netsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA Database\" >nul 2>&1', { stdio: 'inherit' });\n      execSync('netsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Database\" dir=in action=allow protocol=TCP localport=5432 profile=any', { stdio: 'inherit' });\n      \n      // Allow Redis port\n      execSync('netsh advfirewall firewall delete rule name=\"TeamIranVSTeamUSA Redis\" >nul 2>&1', { stdio: 'inherit' });\n      execSync('netsh advfirewall firewall add rule name=\"TeamIranVSTeamUSA Redis\" dir=in action=allow protocol=TCP localport=6379 profile=any', { stdio: 'inherit' });\n      \n      console.log('✅ Windows firewall configured successfully');\n    } catch (error) {\n      console.error('❌ Failed to setup Windows firewall:', error);\n    }\n  }\n\n  // Create Windows database setup\n  async createWindowsDatabaseSetup() {\n    console.log('🗄️ Creating Windows database setup...');\n    \n    const dbSetupPath = path.join(process.cwd(), 'scripts', 'windows-database-setup.js');\n    const dbSetupCode = `\n// Windows Database Setup\nconst { execSync } = require('child_process');\nconst fs = require('fs');\nconst path = require('path');\n\nclass WindowsDatabaseSetup {\n  constructor() {\n    this.dbConfig = {\n      host: process.env.DB_HOST || 'localhost',\n      port: process.env.DB_PORT || 5432,\n      name: process.env.DB_NAME || 'teamiranvsteamusa_prod',\n      user: process.env.DB_USER || 'teamiranvsteamusa',\n      password: process.env.DB_PASSWORD || 'your_password_here'\n    };\n  }\n\n  async setupDatabase() {\n    console.log('🗄️ Setting up PostgreSQL database...');\n    \n    try {\n      // Create database user\n      console.log('👤 Creating database user...');\n      execSync(`psql -U postgres -c \"CREATE USER ${this.dbConfig.user} WITH PASSWORD '${this.dbConfig.password}';\"`, { stdio: 'inherit' });\n      \n      // Create database\n      console.log('🗄️ Creating database...');\n      execSync(`psql -U postgres -c \"CREATE DATABASE ${this.dbConfig.name} OWNER ${this.dbConfig.user};\"`, { stdio: 'inherit' });\n      \n      // Grant privileges\n      console.log('🔐 Granting privileges...');\n      execSync(`psql -U postgres -c \"GRANT ALL PRIVILEGES ON DATABASE ${this.dbConfig.name} TO ${this.dbConfig.user};\"`, { stdio: 'inherit' });\n      \n      console.log('✅ Database setup completed successfully');\n    } catch (error) {\n      console.error('❌ Failed to setup database:', error);\n    }\n  }\n\n  async runMigrations() {\n    console.log('🔄 Running database migrations...');\n    \n    try {\n      const migrationsPath = path.join(__dirname, '..', 'migrations');\n      const migrationFiles = fs.readdirSync(migrationsPath).sort();\n      \n      for (const file of migrationFiles) {\n        if (file.endsWith('.sql')) {\n          console.log(`🔄 Running migration: ${file}`);\n          const migrationPath = path.join(migrationsPath, file);\n          execSync(`psql -h ${this.dbConfig.host} -p ${this.dbConfig.port} -U ${this.dbConfig.user} -d ${this.dbConfig.name} -f ${migrationPath}`, { stdio: 'inherit' });\n        }\n      }\n      \n      console.log('✅ Database migrations completed successfully');\n    } catch (error) {\n      console.error('❌ Failed to run migrations:', error);\n    }\n  }\n\n  async seedDatabase() {\n    console.log('🌱 Seeding database...');\n    \n    try {\n      const seedPath = path.join(__dirname, '..', 'seed', 'seed.sql');\n      execSync(`psql -h ${this.dbConfig.host} -p ${this.dbConfig.port} -U ${this.dbConfig.user} -d ${this.dbConfig.name} -f ${seedPath}`, { stdio: 'inherit' });\n      \n      console.log('✅ Database seeded successfully');\n    } catch (error) {\n      console.error('❌ Failed to seed database:', error);\n    }\n  }\n}\n\nconst dbSetup = new WindowsDatabaseSetup();\n\nmodule.exports = dbSetup;\n`;\n    \n    fs.writeFileSync(dbSetupPath, dbSetupCode);\n    console.log(`✅ Windows database setup created: ${dbSetupPath}`);\n  }\n\n  // Test Windows compatibility\n  async testWindowsCompatibility() {\n    console.log('🧪 Testing Windows compatibility...');\n    \n    const tests = [\n      {\n        name: 'Windows Detection',\n        test: () => this.config.isWindows\n      },\n      {\n        name: 'Command Availability',\n        test: () => {\n          try {\n            execSync('netsh advfirewall show allprofiles', { stdio: 'pipe' });\n            return true;\n          } catch {\n            return false;\n          }\n        }\n      },\n      {\n        name: 'Service Management',\n        test: () => {\n          try {\n            execSync('sc query', { stdio: 'pipe' });\n            return true;\n          } catch {\n            return false;\n          }\n        }\n      },\n      {\n        name: 'File Permissions',\n        test: () => {\n          try {\n            fs.accessSync(process.cwd(), fs.constants.W_OK);\n            return true;\n          } catch {\n            return false;\n          }\n        }\n      }\n    ];\n    \n    let passed = 0;\n    let total = tests.length;\n    \n    for (const test of tests) {\n      console.log(`🧪 Testing: ${test.name}`);\n      try {\n        const result = test.test();\n        if (result) {\n          console.log(`✅ ${test.name}: PASSED`);\n          passed++;\n        } else {\n          console.log(`❌ ${test.name}: FAILED`);\n        }\n      } catch (error) {\n        console.log(`❌ ${test.name}: ERROR - ${error.message}`);\n      }\n    }\n    \n    const successRate = (passed / total) * 100;\n    console.log(`📊 Windows Compatibility Test Results: ${passed}/${total} (${successRate}%)`);\n    \n    if (successRate >= 75) {\n      console.log('✅ Windows compatibility test PASSED');\n      return true;\n    } else {\n      console.log('❌ Windows compatibility test FAILED');\n      return false;\n    }\n  }\n}\n\n// Main execution\nasync function main() {\n  const windowsCompatibilityFixer = new WindowsCompatibilityFixer();\n  \n  console.log('🔧 Starting Windows Compatibility Fixes...');\n  \n  try {\n    const success = await windowsCompatibilityFixer.initialize();\n    if (success) {\n      console.log('🎉 Windows Compatibility Fixes Completed Successfully!');\n      console.log('📋 Next Steps:');\n      console.log('1. Run windows-deploy.bat for deployment');\n      console.log('2. Run windows-service.bat install for service management');\n      console.log('3. Run windows-firewall.bat for firewall configuration');\n      console.log('4. Run windows-database.bat for database setup');\n      process.exit(0);\n    } else {\n      console.error('❌ Windows Compatibility Fixes Failed');\n      process.exit(1);\n    }\n  } catch (error) {\n    console.error('❌ Fatal error in Windows Compatibility Fixes:', error);\n    process.exit(1);\n  }\n}\n\n// Run if called directly\nif (require.main === module) {\n  main();\n}\n\nmodule.exports = WindowsCompatibilityFixer;\n`;\n\n    fs.writeFileSync(scriptPath, scriptCode);\n    console.log(`✅ Windows compatibility fix script created: ${scriptPath}`);\n  }\n}\n\n// Main execution\nasync function main() {\n  const windowsCompatibilityFixer = new WindowsCompatibilityFixer();\n  \n  console.log('🔧 Starting Windows Compatibility Fixes...');\n  \n  try {\n    const success = await windowsCompatibilityFixer.initialize();\n    if (success) {\n      console.log('🎉 Windows Compatibility Fixes Completed Successfully!');\n      process.exit(0);\n    } else {\n      console.error('❌ Windows Compatibility Fixes Failed');\n      process.exit(1);\n    }\n  } catch (error) {\n    console.error('❌ Fatal error in Windows Compatibility Fixes:', error);\n    process.exit(1);\n  }\n}\n\n// Run if called directly\nif (require.main === module) {\n  main();\n}\n\nmodule.exports = WindowsCompatibilityFixer;\n
